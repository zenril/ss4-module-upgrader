import { IArgumentCallback } from './IArgumentCallback';
import { Argument } from './Argument';
import { IDefaultCallback } from './IDefaultCallback';
export declare class ArgumentManager {
    private shorts;
    private longs;
    private defaults;
    private registeredShorts;
    private registeredLongs;
    constructor();
    private getValue(arg, value);
    private indexOf(arr, arg);
    private callArg(lngShrt, arg, defaultValue, callback);
    private remove(arg);
    onDefault(callback: IDefaultCallback): void;
    on(arg: Argument, callback: IArgumentCallback): void;
    private register(arg);
    private checkRegistered(arg);
}
export { Argument } from './Argument';
